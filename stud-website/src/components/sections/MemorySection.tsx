export default function MemorySection() {
  return (
    <section className="mx-auto w-full max-w-7xl py-16">
      <p className="font-base mx-auto max-w-4xl text-center text-2xl leading-relaxed tracking-tight text-balance md:text-3xl">
        <span className="font-base text-muted-foreground tracking-tight">
          PlayerZero builds a <span className="text-foreground">memory</span> of
          every problem, so that the same mistake is{" "}
          <span className="text-foreground">never made twice</span>.
        </span>
      </p>
    </section>
  );
}
